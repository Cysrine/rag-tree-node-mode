"""Phase 4/5: Postgres + pgvector storage.

schema.sql is the source of truth for the DDL (also auto-applied by docker-compose).
The Repository (inserts + recursive path query + vector search) is a stub until
Phase 4.
"""
